# 个人月历 V2 原生视觉与交互清单

清单更新日期：2026-08-04（Asia/Shanghai）

验收对象来自最终 `dist/个人月历.app.zip` 新鲜解包的真实可执行文件；没有使用 `.build/debug` 代替。为了与用户正在运行的同 Bundle ID 应用隔离，临时壳仅更改 `Info.plist` 身份；最终可执行与临时壳可执行 SHA-256 均为 `60eeff08157dd56d60f6e852fa50cae939f4eb9209b5e898966531cd8024f384`。临时壳不作为签名证据，因为修改 bundle 身份会使原签名失效；未修改的 ZIP／DMG app 已分别通过严格签名与 CDHash 验证。

| # | 原生检查步骤 | 实际记录 | 状态 |
| --- | --- | --- | --- |
| 1 | 依次点击窗口左上、右上、左下、右下附近日期空白 | 四张卡片都跟随日期，靠边自动翻向可用空间；标题框自动聚焦 | PASS |
| 2 | 月末正向拖到下月，再从下月反向拖回月末 | 两个方向都有连续高亮；松手均预填 `2026-07-31 → 2026-08-04` | PASS |
| 3 | 全屏向前、向后连续滚动各至少 52 周 | 到达 2027-09 与 2023-08，标题跟随中心周；重复 Today 后可再次手动滚到 2026-10，无滚动锁死 | PASS |
| 4 | 同一天建立 11 条事项，包含跨日与单日 | 10 条直接可见，第 11 条折叠为“还有1项”；抽屉 11 条唯一来源，跨日来源不重复 | PASS |
| 5 | 拖普通跨日条带主体、左右外端 handle，并分别 Command-Z | 主体移动保持跨度；右端只改结束；左端只改开始（本次坐标拖动跨过多日，但未改变另一端）；各操作撤销完整恢复 | PASS |
| 6 | 重复跨日分别选“仅本次”“本次及以后”“取消”，并制造保存失败后重试 | 取消哈希零变化；仅本次生成首例例外；本次及以后分裂为新规则；保存失败不覆盖旧文件，重试只写一条 | PASS |
| 7 | 建立范围与完成状态，彻底退出并重启最终包 | 范围、完成和来源身份保持；启动位置仍居中今天 | PASS |
| 8 | 恢复 schema 1 完整备份，再尝试语义无效备份 | 有效恢复迁移为 schema 2 且保留完整图；无效恢复前后 primary SHA-256 完全一致 | PASS |
| 9 | 浅／深色、五组色板、极端自定义色、VoiceOver、减少动态 | 基础／马卡龙／莫兰迪／自然／鲜亮各 8 色；黑白极值均有浅深对比度；系统实际开启 VoiceOver 与减少动态完成验收，并恢复原设置 | PASS |

## 关键可复核数值

- 重复操作取消前后 SHA-256：`bfc214a1b07d6f1bdc3566361233ad7ccbb8627870a8a65adf7122b6510c6320`。
- 重复“仅本次”后 SHA-256：`9d7ed9ceefeb7fe9d4a09a6b8ccb6750c99eb8d3f9340aa52aa013cc2a56e223`。
- 重复“本次及以后”后 SHA-256：`d8bb6ffb0a5a88b77afbd77517f55376ea4b9d13f038eb29546e9d0406c8f8f4`。
- 保存失败前／失败后 SHA-256：`edad5b36d253dfd4d802859a494b1c8f63a88874c758da952fc00ef33e511b05`；重试后为 `4c7ab5b0e4efee5a782a741643cddeaeb7e82bc82381521380e885cc1db77386`，磁盘中同名事项计数为 1。
- V1 有效恢复后／无效恢复后 SHA-256：`28c1983080936e78205f15d1165954fc2e75fa5c40b298b88afee6744abd1c5f`。
- 自定义黑色预览：浅色文字 `9.90:1`、浅色强调 `13.58:1`、深色文字 `14.96:1`、深色强调 `3.08:1`。
- 自定义白色预览：浅色文字 `13.85:1`、浅色强调 `3.08:1`、深色文字 `9.23:1`、深色强调 `10.72:1`。

## 系统设置恢复

- 外观：验收前为深色；浅色截图完成后已恢复深色。
- 减弱动态效果：验收前为关闭；开启并完成远距 Today 对齐后已恢复关闭。
- VoiceOver：验收前为关闭；开启并完成 AX 语义核验后已恢复关闭。
- System Settings 验收窗口已关闭；隔离验收应用已退出。

## 证据原则

- 所有截图都位于 `docs/validation/calendar-v2/evidence/`，文件名以 `final-` 开头的是最终制品结果。
- `restart-wrong-initial-position.jpeg` 等未带 `final-` 的文件保留修复前失败或中途验证轨迹；后续成功没有覆盖这些证据。
- 原生鼠标拖选、条带拖动、两端缩放均通过真实 GUI 执行；纯函数测试没有替代这些步骤。
- VoiceOver 与减少动态是系统设置中真实开启后执行，不是只引用源码或单测。
